<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PasarCepat">

        <activity
            android:name=".presentation.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".presentation.MainActivity"
            android:exported="false" />
        
        <activity android:name=".presentation.DetailActivity" />
        <activity android:name=".presentation.CartActivity" />
        <activity android:name=".presentation.SuccessActivity" />
        <activity android:name=".presentation.WishlistActivity" />
        <activity android:name=".presentation.OrderActivity" />
        <activity android:name=".presentation.AccountActivity" />
        <activity android:name=".presentation.NewsActivity" />
        <activity android:name=".presentation.DetailNewsActivity" />
        <activity android:name=".presentation.SearchActivity" />
        <activity android:name=".presentation.SellerActivity" />
        <activity android:name=".presentation.RegisterActivity" />
        <activity android:name=".presentation.CompleteProfileActivity" />
        <activity android:name=".presentation.ReviewActivity" />
        <activity android:name=".presentation.CategoryProductActivity" />
    </application>

</manifest>